DROP THE 5 DYNASTY MONK IMAGES HERE — the homepage picks them up automatically.

Save each monk with EXACTLY these filenames (all lowercase, .png):

  vddvirtue.png     -> VDDVirtue card
  vddjester.png     -> VDDJester card
  vddsaucesage.png  -> VDDSauceSage card
  vddsoloshen.png   -> VDDSoloshen card
  vddcleanse.png    -> VDDCleanse card

Plus the big banner image for the Monks section (landscape JPG):

  monks-banner.jpg  -> large picture in "The Dynasty Monks" section
                       (1200x800px / 3:2 landscape, JPG, under 250 KB)

Tips:
- Square-ish images work best (they show in a round avatar, cropped from the top).
- PNG with transparent background looks nicest; JPG also works if renamed to .png? No —
  keep the .png extension matching the actual file format. If your files are .jpg,
  tell Claude and the code will be updated to match.
- Until a file exists, the card shows the monk's emoji instead.
- No code changes needed — just save the file and refresh the page.
